# https://domoreexp.visualstudio.com/Teamspace/_build?definitionId=2832
#
# A pull request validation build.
# Builds Electron with the "vscode" flavour and runs JavaScript tests.

trigger: none

jobs:
- template: templates/build/build_and_test_electron.yml
  parameters:
    flavour: vscode
